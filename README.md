
# Project Structure

```
elk-dj/
├── docker-compose.yml
├── django/ (Django project)
│   ├── Dockerfile
│   └── manage.py 
├── logstash/
│   └── pipeline.conf
└── elasticsearch/
    └── elasticsearch.yml
```
